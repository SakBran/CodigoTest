export interface RawMaterial {
  rawMaterialID: string;
  rawSetupID:string;
  rawMaterialName: string;
  rawMaterialQty: number;
  rawMaterialUnitPrice: number;
  rawMaterialTotalPrice: number;
  productID: string;
}
